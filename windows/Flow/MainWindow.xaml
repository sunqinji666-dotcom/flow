<Window x:Class="Flow.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Flow" Height="700" Width="380"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Color x:Key="AccentCyan">#45D6FF</Color>
        <Color x:Key="AccentGold">#F3B85B</Color>
        <Color x:Key="TextPrimary">#F2F7FF</Color>
        <Color x:Key="TextSecondary">#71859B</Color>
        <Color x:Key="GreenOn">#34C759</Color>
        <Color x:Key="Danger">#FF6B5F</Color>
        <Color x:Key="BgCard">#151B24</Color>
        <Color x:Key="BgDeep">#090B10</Color>
        <SolidColorBrush x:Key="AccentCyanBrush" Color="{StaticResource AccentCyan}"/>
        <SolidColorBrush x:Key="AccentGoldBrush" Color="{StaticResource AccentGold}"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimary}"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondary}"/>
        <SolidColorBrush x:Key="GreenOnBrush" Color="{StaticResource GreenOn}"/>
        <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource Danger}"/>
    </Window.Resources>

    <Border CornerRadius="16" Background="#E6151B24" BorderBrush="#20FFFFFF" BorderThickness="1">
        <Grid Margin="0,32,0,0">
            <!-- Title bar drag area -->
            <Grid Background="Transparent" VerticalAlignment="Top" Height="32" MouseDown="TitleBar_MouseDown"/>

            <DockPanel Margin="24,8,24,16">
                <!-- Top: brand + status -->
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBlock Text="Flow" FontSize="19" FontWeight="ExtraBold" FontStyle="Italic" Foreground="{StaticResource AccentCyanBrush}" VerticalAlignment="Center"/>
                    <Rectangle Width="*"/>
                    <Border CornerRadius="12" Background="#14FFFFFF" Padding="12,6">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="7" Height="7" Fill="{StaticResource GreenOnBrush}" x:Name="StatusDot" Margin="0,0,6,0"/>
                            <TextBlock Text="准备就绪" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" x:Name="StatusLabel"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <ScrollViewer DockPanel.Dock="Top" VerticalScrollBarVisibility="Hidden">
                    <StackPanel>
                        <!-- Connect button -->
                        <Button x:Name="ConnectBtn" Width="148" Height="148" Margin="0,0,0,16" Click="ConnectBtn_Click"
                                HorizontalAlignment="Center" Style="{DynamicResource CircleButtonStyle}">
                            <TextBlock Text="连" FontSize="48" FontWeight="ExtraBold" Foreground="{StaticResource AccentGoldBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" x:Name="ConnectBtnLabel"/>
                        </Button>

                        <!-- Node info -->
                        <TextBlock Text="🌐 443-dtfnwuzy" FontSize="22" FontWeight="ExtraBold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center" x:Name="NodeNameLabel"/>
                        <TextBlock Text="VLESS · TCP · example.com:443" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0" x:Name="NodeDetailLabel" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>

                        <!-- Node selector button -->
                        <Button Margin="0,10,0,0" Click="NodePicker_Click" Style="{DynamicResource CardButtonStyle}">
                            <Grid>
                                <TextBlock Text="选择节点 ▼" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                            </Grid>
                        </Button>

                        <!-- Proxy mode banner -->
                        <Border CornerRadius="12" Background="#0DFFFFFF" Padding="10,7" Margin="0,8,0,0">
                            <Grid>
                                <StackPanel>
                                    <TextBlock Text="本地端口模式" FontSize="11" FontWeight="ExtraBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                    <TextBlock Text="不改系统代理，其他软件手动填端口" FontSize="9" Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Stats pills -->
                        <UniformGrid Columns="2" Margin="0,8,0,0">
                            <Border CornerRadius="20" Background="#0AFFFFFF" Padding="12,8" Margin="0,0,4,0">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="7" Height="7" Fill="{StaticResource GreenOnBrush}" Margin="0,0,6,0"/>
                                    <TextBlock Text="状态" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"/>
                                    <TextBlock Text="准备就绪" FontSize="12" FontWeight="ExtraBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="6,0,0,0" x:Name="StatusPillText"/>
                                </StackPanel>
                            </Border>
                            <Border CornerRadius="20" Background="#0AFFFFFF" Padding="12,8" Margin="4,0,0,0">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="7" Height="7" Fill="{StaticResource GreenOnBrush}" Margin="0,0,6,0"/>
                                    <TextBlock Text="时长" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"/>
                                    <TextBlock Text="00:00" FontSize="12" FontWeight="ExtraBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="6,0,0,0" x:Name="DurationPillText"/>
                                </StackPanel>
                            </Border>
                        </UniformGrid>

                        <!-- Traffic boxes -->
                        <UniformGrid Columns="3" Margin="0,6,0,0">
                            <Border CornerRadius="10" Background="#0AFFFFFF" Padding="8,7" Margin="0,0,3,0">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="本次" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                                    <TextBlock Text="0 MB" FontSize="13" FontWeight="ExtraBold" Foreground="{StaticResource AccentGoldBrush}" HorizontalAlignment="Center" x:Name="SessionTrafficLabel"/>
                                    <TextBlock Text="连接后统计" FontSize="8" Foreground="#4E5965" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <Border CornerRadius="10" Background="#0AFFFFFF" Padding="8,7" Margin="2,0,3,0">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="今日" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                                    <TextBlock Text="0 MB" FontSize="13" FontWeight="ExtraBold" Foreground="{StaticResource AccentGoldBrush}" HorizontalAlignment="Center" x:Name="TodayTrafficLabel"/>
                                    <TextBlock Text="今天已用" FontSize="8" Foreground="#4E5965" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <Border CornerRadius="10" Background="#0AFFFFFF" Padding="8,7" Margin="3,0,0,0">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="累计" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                                    <TextBlock Text="0 MB" FontSize="13" FontWeight="ExtraBold" Foreground="{StaticResource AccentGoldBrush}" HorizontalAlignment="Center" x:Name="TotalTrafficLabel"/>
                                    <TextBlock Text="设备累计" FontSize="8" Foreground="#4E5965" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </UniformGrid>
                    </StackPanel>
                </ScrollViewer>

                <!-- Gear Button -->
                <Button DockPanel.Dock="Bottom" Width="44" Height="44" Margin="0,10,0,0" Style="{DynamicResource CircleButtonStyle}" HorizontalAlignment="Center"
                        Click="Settings_Click" VerticalAlignment="Bottom">
                    <TextBlock Text="⚙" FontSize="18" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button>
            </DockPanel>
        </Grid>
    </Border>
</Window>
